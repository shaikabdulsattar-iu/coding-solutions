#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);
    
    while (t--) {
        int n, m;
        scanf("%d %d", &n, &m);
        
        // Calculate total capacity
        int total_people = (5 * n) + (7 * m);
        
        printf("%d\n", total_people);
    }

    return 0;
}
